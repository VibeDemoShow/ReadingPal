// Dummy screen — never actually rendered.
// The tab bar button triggers logout instead of navigating here.
import { View } from 'react-native';
export default function LogoutScreen() {
  return <View />;
}
